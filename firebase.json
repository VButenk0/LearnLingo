{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null",
        "favorites": {
          ".validate": "newData.exists()"
        }
      }
    },
    "teachers": {
      ".read": true,
      ".write": false
    }
  }
}
